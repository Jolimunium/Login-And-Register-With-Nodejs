<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <div class="navbar-nav">
                <a class="nav-link" href="/">Home Page</a>
            </div>
            <a class="navbar-brand mx-auto">Home Page</a>
            <div class="navbar-nav">
                <% if (typeof user !== 'undefined') { %>
                    <a class="btn btn-outline-success mx-2" href="/logout">logout</a>
                <% } else { %>
                    <a class="btn btn-outline-success mx-2" href="/login">Login</a>
                    <a class="btn btn-success" href="/register">Register</a>
                <% } %>
            </div>
        </div>
    </nav>
    <div class="d-flex justify-content-center align-items-center" style="min-height: calc(100vh - 56px);">
        <div class="card shadow p-4" style="width: 350px;">
            <% if (typeof user !== 'undefined') { %>
                <h3 class="text-center mb-3">You are login</h3>
            <% } else { %>
                <h3 class="text-center mb-3">You are not login</h3>
            <% } %>
        </div>
    </div>

</body>

</html>